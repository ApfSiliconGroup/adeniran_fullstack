# Where the FastAPI backend lives.
#
# Local development:            http://localhost:8000
# Single Vercel project:        /api      (see the repository root vercel.json)
# Separately hosted backend:    https://your-api-host.example.com
NEXT_PUBLIC_API_URL=http://localhost:8000
